# load the following source lists from dotfiles repo and from system files:
	#apt repos ( automatic(?) & manual)
	#apt packages
	#apps in /usr/bin
	#repos in ~/src
	#node packages
	#pip packages
	#snap packages

# diff lists and output ones that are missing from system

# checklist to select desired packages sorted by package manager

# attempt to source missing packages & maybe create install script

 
	
