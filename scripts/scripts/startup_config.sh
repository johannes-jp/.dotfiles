# configure one-time settings on system setup that may require elevated privileges or can't be handled by stow

# symlink apt source lists in /etc/apt

# auto-mount shared folders (VM only)
	# Obsidian
	# files/public (various files)
